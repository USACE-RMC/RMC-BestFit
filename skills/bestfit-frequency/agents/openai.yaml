interface:
  display_name: "BestFit Analyses and Plots"
  short_description: "Use saved BestFit results and app-style plots"
  default_prompt: "Use $bestfit-frequency for the requested BestFit analysis or saved-result plots, preserving source settings and displaying the figure."
policy:
  allow_implicit_invocation: true
