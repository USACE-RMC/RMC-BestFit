# v2.0.1 Report and Verification Closeout Implementation Plan

> **For agentic workers:** Use `superpowers:executing-plans` to complete the remaining sessions in order. Steps use checkboxes for the release handoff. This plan is a work-control document and is excluded from both published report books.

**Goal:** Deliver v2.0.1 with polished technical-reference and verification-report drafts, a reconciled verification library, and release-package evidence by 28 September 2026, in no more than three sessions.

**Architecture:** The current repository defines the software described by both reports. Markdown is the publication source; the chapter manifests produce the PDFs. The method catalog and generated test index connect scientific claims to executable verification methods without adding project history to the public narrative.

**Tech stack:** .NET 10, C#, MSTest, PowerShell, Python, Markdown, Node.js, LaTeX/dvisvgm, and Chromium.

**Scope:** Haden Smith's 21 September request to review the branch and supporting Numerics work, finalize the reports and verification library in at most three sessions, and start by polishing Verification XML and delivering a fresh, complete verification report.

## Constraints and release target

- Haden Smith retains final numerical authority. This closeout does not authorize algorithm, likelihood, prior, seed, tolerance, convergence, or reference-result changes.
- Preserve the current scientific scope. The two accepted limitations and the distinction between recovery and repeated-sample coverage remain explicit.
- The public reports describe current behavior, equations, designs, results, and limitations. Historical findings and execution records remain supporting work documents.
- Run Core, UI, App, and API fast suites after each session's changes. XML work also requires the strict XML gate. Numerical checks stay limited to the affected scope; runs of record use the exact-method runner and one-result TRX rule. No blanket Verification run is required by this plan.
- Preserve unrelated changes in `C:/GIT/Numerics/.github/workflows/Release.yml`.
- Target Session 2 by 23 September and Session 3 by 25 September. Reserve 26-28 September for review, infrastructure delays, and publication. Complete release preparation before requesting any final publication decision.

## Reviewed baseline and implications

BestFit branch: `documentation-verification-updates`, source baseline `8808f19f3bfa712241e55a0dfa47407ff5a60970`. The local `origin/main` reference is `0dfbfad78273458cc59f64729f48677b1b6089fe`; its merge base with this branch is `6a195e4424f0c4ebe690467e41bad21480b0c2cd`. The change-scope review covered all 126 branch-only commit subjects and change statistics, together with the current verification sources, catalog, report chapters, and relevant implementation paths. This is release and documentation reconciliation, not a claim that every changed line in the application has received a new independent audit.

Supporting Numerics baseline: `7e8e8d1c5f26e045a35ec9fc09367de95ed05b02`, with a 226-commit history since `v2.1.4` and the 2.2.0 release preparation at `0c3b495`. The current declared BestFit dependency is RMC.Numerics 2.2.0. Local builds prefer the sibling project, so their success does not establish that a clean package-only release resolves the same dependency.

| Change area reviewed | Representative commits | Closeout implication |
|---|---|---|
| Independent oracles and diagnostic contracts | BestFit `5d975b7`, `6ffab67`; Numerics `d5f8e46`, `6c551d1` | Describe analytical/external references, objective conventions, modern R-hat, and conservative ESS. |
| Point processes, mixtures, competing risks, composites | BestFit `a6918b5`, `c3db43b`, `a02c8a8`, `ff17257`; Numerics `3e69a93`, `cafe6cf` | Use current generating models and identified response quantities; retain the dependent and boundary-case limitations. |
| Time-series likelihood, transforms, recovery, prediction | BestFit `65045e0`, `dbf16d7`, `6cc26d0`, `808be5c` | Distinguish conditional likelihood from Kalman/state-space comparisons and parameter uncertainty from prediction uncertainty. |
| Rating curves and spatial likelihood/prediction | BestFit `4bc095e`, `134643a`, `dc27300`, `e1ee6eb`, `503fd9c`, `808be5c` | Document discharge-space density, simultaneous predictive bands, missing-site likelihoods, and the implemented spatial uncertainty methods. |
| Distribution robustness, constraints, numerical defaults | BestFit `9a8d969`, `81882ce`, `7059034`; Numerics `b8bf912`, `46792b4`, `2b57771`, `e485640` | Reconcile source parameterizations and actual defaults; preserve numerical-roundoff versus inferential acceptance distinctions. |
| B17C Jacobians, matrix repair, optimizer behavior | BestFit `4732d5c`, `cc5931c`, `99c97a4`; Numerics `189a597`, `156204f`, `c949397` | Existing analytical Jacobians and checked Cholesky/ridge candidates are already implemented. The supplied-gradient roundoff check retains the existing gradient tolerance. Accepted bootstrap output is distinct from convergence of the outer GMM iteration. |
| Current verification ownership | BestFit `91f001a`, `0a2703a`, `2bd8efb`, `ae4aaa1` | Use 328 current methods, not the older 384-entry inventory; preserve removed entries outside the current catalog. |
| Saved-project, data import, API, and release integration | BestFit `b732703`, `d367f06`, `3e0371a`, `8808f19` | Keep compatibility and import regressions in the fast gate; include API/MGBT and example changes in the release notes without implying new numerical verification. |

The three source documents requested for review have distinct roles: the existing [finalization plan](verification-finalization-plan.md) preserves earlier phase decisions; the [completeness audit](verification-completeness-audit.md) now identifies the current source denominator; the [review findings](../technical-reference/review-findings.md) preserve scientific dispositions. Their historical dates and counts must not drive the fresh report narrative.

## Review focus

1. Package-only builds must resolve Numerics 2.2.0 without the sibling checkout; Session 3 checks assets and the packed dependency.
2. A deleted test or changed fixture must not leave a current report claim behind; Sessions 1 and 2 check source/catalog/index identities and acceptance rules.
3. Accepted B17C refits must not be reported as universal convergence or interval coverage; Sessions 1 and 2 check that distinction against the recorded 1,000-fit experiment.
4. Markdown equations must remain readable in GitHub and in a downloaded PDF; each report build needs math parsing, complete vector glyphs, and page review.
5. v2.0.1 publishing must not fail the existing v2.0.0-only workflow guard; Session 3 updates and locally exercises version validation before any release event.
6. Resolve the Viglione systematic-only case's Q1000 lower-endpoint reference before claiming complete published-source parity. The current test target is 163 m³/s; Skahill et al. (2016), Table 2 (page 5), gives 183 m³/s. The v1 report's Table 83 places 163 in the BestFit column and 183 in the external-source column. The current report explicitly limits this endpoint's claim. Changing its reference-result contract requires Haden Smith's prior decision under AGENTS.md; this documentation work does not authorize that change.

## Session 1 - Verification report and XML (completed 21 September 2026)

**Files:** `src/RMC.BestFit.Verification/**/*.cs`; `docs/verification/report/*.md`; `docs/verification/verification-catalog*.json`; `docs/verification/README.md`; `docs/verification/publication-quality.md`; `docs/report-metadata.json`; `scripts/build-verification-*`; `scripts/validate-equation-assets.py`; `scripts/validate-code-xml-docs.ps1`; `scripts/pdf_report_common.py`; `src/RMC.BestFit.Tests/Documentation/TechnicalReferenceDocumentationTests.cs`; `output/pdf/rmc-bestfit-verification-report.pdf`.

- [x] Audit the entire current source inventory: 328 methods in 71 active classes, 326 verified dispositions, and two accepted limitations.
- [x] Polish XML comments and citations; independently confirm all 37 changed Verification C# files retain identical executable source and test declarations.
- [x] Reconcile every scientific chapter with source and catalog acceptance rules. Follow the v1 report's reader-facing approach: every analysis must explain its data, setup, procedure, numerical results, and interpretation without requiring code knowledge. Remove historical progress narration from the public report.
- [x] Generate Appendix A with every method and its owning source file, including partial classes. Preserve 56 removed records separately without counting them as active tests.
- [x] Render and visually review the revised PDF, with 95 reviewed pages, 212 complete equations, printed contents page numbers, 20 bookmarks, and the current catalog embedded as an attachment.
- [x] Pass the strict XML gate across 949 source files, all 5,038 fast tests, all ten final documentation contracts, and the complete-catalog/index checks.

**Deliverable:** The current verification draft and its [publication-quality record](publication-quality.md). This editorial closeout does not claim a fresh execution of all 328 numerical methods.

## Session 2 - Technical-reference draft and final evidence reconciliation

**Files:** `docs/technical-reference/front-matter.md`, `book-order.txt`, `api-traceability.md`, `appendices/verification-evidence.md`, `appendices/implementation-source-index.md`, affected `analysis/`, `distributions/`, `estimation/`, and `spatial/` chapters; `docs/report-metadata.json`; `scripts/build-technical-reference-book.mjs`, `scripts/build-technical-reference-book.ps1`; `output/pdf/rmc-bestfit-technical-reference.pdf`; current catalog and report only if a concrete mismatch is found.

- [ ] Read this plan, both report manifests, the current catalog, and the source-review table above. Use the public API traceability matrix to bound the chapter sweep.
- [ ] Reconcile the technical reference's source baseline and dependency version. Concentrate edits on robust distribution tails/constraints, optimizer defaults and BFGS fallback, B17C analytical Jacobians and accepted/converged semantics, modern diagnostics, time-series conditioning, and current rating/spatial uncertainty behavior.
- [ ] Replace obsolete implementation descriptions and work-log prose with present-tense explanations. Keep mathematical derivations, notation, assumptions, and references next to the behavior they explain.
- [ ] Verify each numerical claim against its source method and unchanged catalog rule. Where current evidence does not support a stronger claim, state its actual scope. A new numerical defect becomes a specific finding for Haden, not an unplanned repair or a relaxed test.
- [ ] Obtain Haden's technical disposition of the Viglione case-1 lower endpoint: published 183 m³/s versus retained 163 m³/s. If a correction is approved, update that exact reference contract and run the affected method through the focused runner; otherwise retain the explicit qualification in both drafts.
- [ ] If a changed dependency makes an affected result uncertain, list the exact methods and run only that relevant scope. Store evidence-grade results through `scripts/run-verification-test.ps1`; do not convert a documentation audit into a full-library rerun.
- [ ] Apply the verified equation-glyph and print-layout controls to the technical-reference builder where needed. Rebuild the PDF, check GitHub-compatible math, and review every page plus dense equation/table pages at full size.
- [ ] Run the documentation contracts, strict XML gate if code comments change, all four fast suites, and catalog/index checks. Save report hashes and the exact source/dependency checkpoint.

```powershell
dotnet test src/RMC.BestFit.Tests -c Debug -- --filter "FullyQualifiedName~RMC.BestFit.Tests.Documentation.TechnicalReferenceDocumentationTests"
.\scripts\validate-verification-catalog.ps1 -Catalog docs/verification/verification-catalog.json -SourceRoot src/RMC.BestFit.Verification -RequireComplete
python scripts/build-verification-coverage.py --check
.\scripts\build-technical-reference-book.ps1
```

**Exit:** Both drafts describe the same intended release behavior and dependency; their inventories and evidence boundaries agree; Markdown and PDFs are readable. Outstanding research stays explicitly outside the release claims.

## Session 3 - v2.0.1 release candidate and publication handoff

**Files:** `Directory.Build.props`, `Directory.Packages.props` if resolution requires a correction, `.github/workflows/NuGetPublish.yml`, `.github/workflows/Integration.yml`, `README.md`, `docs/release-notes-pending.md`, both report front matters and metadata, and `output/pdf/`. Use `output/release/v2.0.1/` for the staged release assets and checksums.

- [ ] Confirm the intended merge baseline and review any commits added since Session 2. Resolve concrete compatibility issues before the release build; do not silently merge or push.
- [ ] Set BestFit package/file version to 2.0.1 and reconcile the assembly-version policy with the existing compatibility tests. Update README installation examples and release notes to the actual shipped behavior.
- [ ] Replace the hard-coded v2.0.0-only NuGet release guard with validation for this release's approved version. Remove the stale `/p:Version=2.0.0` integration-build override. Exercise the accepted `v2.0.1` and rejected malformed/mismatched tag cases locally before any publishing event.
- [ ] Explicitly restore, build, and test with `UseLocalRmcNumerics=false`. Inspect `project.assets.json` and the packed `.nuspec` for the intended Numerics dependency; a local-source build is not the release gate.
- [ ] Compile Verification against the same package without running the full numerical suite. If this reveals a dependency API mismatch, diagnose it before proceeding.
- [ ] Build the model package and desktop/API release deliverables required for v2.0.1. Smoke-check saved-project opening, DSS import, and the documented API/MGBT path using the existing fixtures and instructions; keep scope limited to release compatibility.
- [ ] Regenerate both report PDFs at the final source checkpoint and label them consistently as drafts included with v2.0.1. Review any changed pages and verify complete equations, links, bookmarks, index identities, and attachment metadata.
- [ ] Stage both PDFs, the release packages, release notes, and SHA-256 checksums under `output/release/v2.0.1/`. Verify the actual asset list before preparing the release description. Do not assume the existing NuGet workflow attaches either PDF: it currently only publishes the model package.
- [ ] Present the concrete release candidate, test counts, dependency resolution, both drafts, retained limitations, and any external-environment gaps. Commit, push, tag, merge, or publish only within the user's authorization for that operation.

```powershell
dotnet restore RMC.BestFit.sln -p:UseLocalRmcNumerics=false
dotnet build RMC.BestFit.sln -c Release --no-restore -p:UseLocalRmcNumerics=false
dotnet test src/RMC.BestFit.Tests -c Release --no-build -p:UseLocalRmcNumerics=false
dotnet test src/RMC.BestFit.UI.Tests -c Release --no-build -p:UseLocalRmcNumerics=false
dotnet test src/RMC.BestFit.App.Tests -c Release --no-build -p:UseLocalRmcNumerics=false
dotnet test src/RMC.BestFit.Api.Tests -c Release --no-build -p:UseLocalRmcNumerics=false
dotnet build src/RMC.BestFit.Verification -c Debug -p:UseLocalRmcNumerics=false
dotnet pack src/RMC.BestFit/RMC.BestFit.csproj -c Release --no-build --no-restore -p:UseLocalRmcNumerics=false -o output/release/v2.0.1
```

**Exit:** A reproducible v2.0.1 candidate includes both polished report drafts, passes all four fast suites with the release dependency, and has a reviewed publication path. Scientific scope does not expand to make the calendar.

## Fixed scope and unresolved boundaries

The release does not add broad B17C interval coverage, calibrated GMM exact-deletion magnitudes, generic coupled-prior sampling, or an outer-GMM damping/convergence redesign. The recorded B17C experiment has 1,000 accepted refits, 947 outer-converged fits, and 53 capped fits; this distinction must survive release wording. Administrative report numbers and named scientific reviewers may remain unassigned in a clearly labelled draft; they must not be presented as completed external review.

If a release blocker appears, use the reserved days for that identified blocker. Defer new research, performance expansion, unrelated CI repairs, new platforms, and broad test-library restructuring beyond v2.0.1.
