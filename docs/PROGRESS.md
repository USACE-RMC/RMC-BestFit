# Progress

## 2026-09-22

- Completed Session 2 technical-reference finalization: the [finished PDF](../output/pdf/rmc-bestfit-technical-reference.pdf) has 249 pages, 66 chapters, 68 bookmarks, and 1,256 unique vector equations. The [publication-quality record](technical-reference/publication-quality.md) records the full chapter audit, final hash, visual review and evidence boundaries. The manual adds worked explanations of prior/data precision, transformed moments, GMM covariance, B17C Jacobians, and predictive uncertainty, with current source/API traceability throughout.
- Separate MathJax 3.2.2 and offline LaTeX/SVG checks account for all 1,940 equation occurrences. Page-by-page review and enlarged inspection of dense equations/tables are complete. The shared finalizer now resolves chapter anchors and numbers multiple contents pages; three focused navigation tests pass. The final PDF has no unresolved link, glyph, text-overflow, metadata, or navigation findings.
- Validation passed Core 3,434 / UI 645 / App 444 / API 515 (5,038 total, zero failures/skips), 10 documentation contracts, strict Debug XML/build checks across 949 source files, the 328-method catalog and complete index, and the 84-source bibliography. Dependency mode was local Numerics 2.2.0 at `7e8e8d1c5f26e045a35ec9fc09367de95ed05b02`; the reviewed BestFit source is `3fa55a0f75bbd583e2fb7fce42180faa376f007c`. No numerical Verification test or production C# change was made.
- Preserved the verification PDF at SHA-256 `8c842772cafdcd60952a836dc2a8da2c65c4a03d597571a9383e313edc8e354c`; a temporary shared-finalizer regression retained its text, destinations, bookmarks, tagging and catalog attachment. Numerical contracts and the unrelated Numerics workflow edit remain unchanged. The Kamp/Viglione retained 163 versus published 183 m3/s endpoint remains explicitly qualified pending Haden's decision.
- Session 2 is delivered in the scoped local documentation commit. [Session 3](verification/v2.0.1-closeout-plan.md#session-3---v201-release-candidate-and-publication-handoff) remains open for version changes, package-only validation and release assets. No push, merge, tag or publication was performed.

## 2026-09-08

- Implemented the approved [distribution robustness integration](distribution-robustness.md): stable logarithmic quantile Jacobians and mixture EM observation/responsibility arithmetic, effective support for inactive components, and validation of unusable automatic initialization while preserving editable parameters and seasonal defaults. The paired Numerics worktree contains the 15-family plus composite probability, moment, derivative, support and uncertainty repairs, including new GNO/GLO/Kappa local-MLE covariance.
- Combined Release validation after incorporating the current local destination branches passes: Core 3,410 (one test worker to avoid unrelated stopwatch contention), UI 599, App 443; zero compiler warnings/errors. XML/namespace scan passes for 941 C# files. Numerics passes 2,690 tests on each of four frameworks with strict Release/XML builds, preserving its destination branch's serial test settings and Normal NaN quantile compatibility. All three BestFit outputs use the same validated Numerics assembly. Unrelated checkout changes were isolated and backed up; no Verification test or package publication was performed for the distribution repair.
- Fixed incomplete HEC-DSS imports across absent regular-series storage blocks. The shared UI reader now resolves every concrete A/B/C/E/F-matching catalog record, reads native calendar blocks independently, preserves raw timestamps and metadata, rejects malformed or conflicting partial results, and lets `TimeSeriesElement` perform its existing missing-value and period-timestamp normalization exactly once. The DSS selector uses the same complete reader for displayed ranges.
- The original v2 importer had used one whole-record `DssReader.GetTimeSeries` call since `TimeSeriesElement` entered the repository in `0d6821d`; it therefore never handled these sparse records comprehensively. Weekly records exposed a second native behavior: explicit reads derive their seven-day phase from the requested start and overlap the prior record at decade boundaries. BestFit now discovers the stored phase from the first nonempty concrete record, applies it only to the first block, uses calendar boundaries thereafter, and filters each result to its owning block.
- Native temporary-file regressions cover short and multi-decade gaps; daily, weekly, minute, hourly, monthly, and annual storage; leap days, offsets, adjacent boundaries, empty blocks, path forms, malformed results, metadata conflicts, cancellation, download reset/undo behavior, selector ranges, and project persistence. Temporary copies of the supplied Pierre file recover ES 305, LS 2,592, and whole-record 2,897 valid observations exactly; gaps remain `NaN`, table/plot bindings include later years, and save/reopen preserves all imports. The supplied DSS and BestFit SHA-256 hashes remain unchanged.
- Final validation: strict Debug solution build completed with zero warnings and errors; the XML documentation gate passed all 939 source files; the mandatory fast suites passed Core 3,390 / UI 645 / App 444. The final PVH acceptance rerun reproduced the exact ES, LS, and whole-record counts above from temporary source copies.
- Issue #19: restored magnitude-ranked HS plotting positions for explicit exact, uncertain, and interval representatives below their covering perception thresholds. The correction uses a local negative-infinity preparation threshold and preserves the current recurrence, tie handling, strict probability bounds, threshold counts, source observations, likelihoods, and public APIs.
- Valid affected input frames repair once on open before normal listeners attach. Changed positions retain their dirty flag through construction and persist through normal saving; invalid/unaffected frames and direct `DataFrame(XElement)` position preservation retain their existing contracts. Plotting documentation and pending release notes explain the correction and derived-statistic effects.
- Validation from `b732703`: 23 plotting-position tests and 8 SQLite persistence tests pass; required fast gates pass Core 3,390 / UI 599 / App 443. The first full core run exceeded an unrelated batch test's 250 ms timing limit; the isolated test and complete unchanged core rerun passed. Strict Debug XML documentation gate passed (940 source files). The three authorized `HirschStedingerPlottingPositionVerificationTests` methods, `Example4_MatchesPeakFQ`, `Example5_MatchesPeakFQ`, and `Example7_MatchesPeakFQ`, passed serially through the guarded runner, each with exactly one TRX result.
- Actual Chatfield project open/save/reopen on a temporary copy leaves all seven frames with zero reversals. Recalculated positions match beta.5 within `1.2491e-16`; broad and segmented RainOnSnow positions agree, and their above-1,000 positions are unchanged. Application frequency plots were rendered and inspected. Source fields, time-series references, all plot settings, and saved analysis payloads are preserved; the original SQLite SHA-256 is unchanged. Saved analyses are not re-estimated automatically. Independent code review found no blocking issues.

## 2026-08-22

- Phase 7 closeout complete (decisions 22 August 2026, one at a time): TR-084 fixture defect fixed (nonstationary generators now draw each observation from the trend at its own index; Exponential/Logistic truths moved inside the default rate bounds; acceptance is the gross-error gate of four posterior SD with R-hat and ESS after the 1% and 90%-coverage rules were shown not to be deterministic single-seed gates; 16/16 cells pass under defaults); TR-085 fixed (GMM covariance and post-estimate weighting use only the positive-definite floor, no eigenvalue cap; `B17CCovarianceTests` 13/13, GMM regression set 34/34); TR-086 fixed (relative re-centring of bootstrap measurement-error distributions for log-space fits and positive supports; uncertain-data cells 2/2, reliability grid 14/14); TR-087 fixed (censored coverage fixture computes plotting positions; `Bulletin17CDistribution` keeps constraint-based initial values with a validation warning when the ROS estimate is unavailable; coverage cells not rerun by decision); TR-088 closed as reruns-on-request; TR-089 legacy time-series fixtures moved to 1,000 observations and the 22 Bayesian cells assert central 90% credible-interval coverage and R-hat (`LegacyRecoveryAssertions`; 29/29); TR-090 test race fixed. New fast regressions: `GeneralizedMethodOfMomentsCovarianceScaleTests`, `DataFrameBootstrapShiftTests`, `Bulletin17CInitialParameterFallbackTests`. `scripts/validate-code-xml-docs.ps1` restored (strict builds, namespace scan, private-documentation scan). Fast gates Core 3,337, UI 579, App 438, API 498; MANIFEST 29/29 hashes rechecked; plan marker `finalized`; chapter markers normalized; README and Fresh-Session Prompt rewritten; CLAUDE.md annotated for the superseded Numerics 2.1.4 pin.
- Phase 6 Batch 6.6 complete (approved 22 August 2026): TR-059 closed (`ComputeCorrelationHeuristicSiteWeights` with an `[Obsolete]` forwarding alias; remarks of the weights, `ConfigureForProperCoverage`, and the technical reference state the heuristic nature; no numerical change) and TR-060 corrected (`SpatialDistanceMetric` enum on `SpatialGEV`, new `GaussianCopula`/`SpatialRegressionErrors` constructor overloads and `DistanceMetric` properties; Cartesian default bitwise; geodesic haversine kilometres with latitude/longitude validation; carried by `ConfigureForProperCoverage`, `Clone`, and the reduced/resampled factories; `Validate` rejects mismatched components; optional serialized attribute; range prior unchanged in both metrics). New R oracle `geodesic-distance-oracle.json` (recorded before the run); guarded cells: geodesic oracle and Cartesian planar regression pass plus 9 regression cells; fast gates Core 3,328, UI 579, App 438, API 498. Phase 6 closed: chapter marker `phase-6-complete`, plan marker `phase-6-complete`, checkpoint and Fresh-Session Prompt rewritten, README spatial row closed. Phase 7 (closeout: TR-084 through TR-090 dispositions, status reconciliation) is next.
- Phase 6 Batch 6.5 complete (approved 22 August 2026, one decision at a time): TR-054 corrected (ungauged-site prediction applies the conditional Gaussian process of every posterior draw with a seeded conditional residual, `SampleConditionalResidual` default true; new R conditional-GP oracle `spatial-conditional-gp-oracle.json` verifies `GetKrigingPrediction` to `1e-10`), TR-058 corrected (regional bounds are posterior quantiles of the per-draw regional mean quantile), TR-061 corrected (Cholesky-dependent simulation through the fitted copula; `GaussianCopula.GetCorrelationMatrix`), TR-056 corrected (temporal block bootstrap on `CreateResampledModel` replicates with MAP refits, NaN failures, 50% floor, `SpatialGEVBootstrapResults`), TR-062 corrected (`RunAsync` dispatches posterior/inflated/Godambe-Gaussian-draw/bootstrap and records `AppliedUncertaintyMethod`; settings serialized), plus TR-092 (non-finite site parameters give negative-infinite likelihood instead of throwing in the sampler) and TR-093 (latent-error default bounds from the link-space spread, floor 1.0) found by the confirmation runs and fixed with approval. Confirmation: the regional cell (66,486 versus 66,526), the dependence contract (−0.02 versus 0.57), and the latent-error sampler failure on the Batch 6.4 source. Acceptance: kriging oracle cell, six guarded cells (prediction, regional, simulation, three dispatch methods), 15 regression cells; fast gates Core 3,323, UI 579, App 438, API 498. Register (TR-054/056/058/061/062/092/093), chapter, technical reference, appendices, baseline, traceability, release notes, manifest, inventory updated.
- Phase 6 Batch 6.4 complete (approved 22 August 2026, one decision at a time): TR-050/TR-051 corrected (`SpatialGEV.CreateReducedModel` builds each leave-one-site-out fold's training model without the held-out site's data column, coordinates, covariate rows, copula coordinate, and latent error; `RunCrossValidationAsync` fits it with a fold `BayesianAnalysis` carrying the main settings and seed and never refits or mutates the main analysis, so the results are retained), TR-052 corrected (folds predict with the held-out site's covariate rows; `GeneralLinearFunction.PredictWithCovariates` throws for a covariate trend without covariates; the shared prediction helper validates covariates before the posterior loop), TR-053 corrected (`FoldStatus`, `FoldMessages`, `SuccessfulFolds`, `TotalFolds`; NaN metrics for unscored folds; aggregates over successful folds; `InvalidOperationException` when no fold succeeds). Confirmation on the Batch 6.3 source: both new cross-validation cells threw "Analysis must be run before predicting at ungauged locations" from a fresh analysis, and the fast mechanism contracts documented the weight-based leakage and the intercept-only prediction. Acceptance: three guarded cross-validation cells (copula parity with an independently reduced model, held-out covariate row parity, unscored-fold accounting) and five Batch 6.3 spot checks pass; fast gates Core 3,309, UI 579, App 438, API 498. Register, chapter, technical reference, appendices, API traceability, baseline, release notes, inventory updated.

## 2026-08-21

- Reviewed every commit since the public releases (RMC-BestFit `6a195e4`, RMC.Numerics `8286646`) for numerical regressions, unapproved algorithm changes, comment narration, and test legitimacy; the findings and resolutions are recorded as TR-066 through TR-083 in `docs/technical-reference/review-findings.md`, with the approved decisions (no package version increments, local Numerics reference, no run aborts for failed Bulletin 17C refits, consistent seasonal simulator, Numerics competing-risk grid fix, per-observation time-series Jacobian, `max(p, q, b)` conditioning, objective-quality refit gate, bounded MLE Hessian).
- Time series: ARIMAX conditions on `max(p, q, b)` everywhere; ARIMA/AR order setters rebuild the training state; empty conditional sums are invalid fits; pointwise transform Jacobians are per observation (alignment oracle regenerated); residual, prediction-window, and transform-reset guards added; fast CI-width and hand-derived contracts added; the tautological MAP/MLE and AIC/BIC assertions replaced by production-versus-analytical checks.
- Point process: seasonal Gumbel-limit annualization sign, clone rate, seasonal quantile priors, and the simulator's per-season threshold intensities corrected; unequal-intensity recovery fixture added.
- Composite and coincident frequency: Numerics `CompetingRisks.CreateEmpiricalCDF` uses log-spaced bins on the offset axis for any transform (local Numerics commit `1753d2d`); zero inflation is inferred only beyond a `1e-10` tolerance; correlation-matrix edits are undoable; the posterior index cache is locked; the composite regression test uses an independent root-solve expectation.
- Estimation and diagnostics: degenerate PSIS tails report `k = +inf` and unestimated Pareto k counts as unreliable; fewer than eleven draws use the fixed limit; POT frames keep their observation span on series replacement; GMM post-processing and covariance queries no longer throw or mutate the weighting state; profile grids report NaN for failed points; the MLE Hessian uses bounded steps; single-parameter covariance; all-failed fitting reports completion; RMSE guards.
- Bulletin 17C: bootstrap diagnostics are per requested replicate with a realization count; the report states substituted replicates as a point mass; the converged-within-tolerance gate requires objective improvement; bound repairs and z-limit clips are counted; log-scale penalty centers are perturbed on the log scale; coverage assertions re-enabled; documentation formulas and comments corrected.
- Comment hygiene: encoding-damaged comment characters repaired in 22 source files; history narration removed from production and test comments; phase/finding-named test classes renamed for their contracts; Numerics copula test references stated correctly.
- Verification tests use the default `BayesianAnalysis` MCMC settings; run one method per `dotnet test` invocation. With defaults, `CoincidentFrequencyAnalysisTests` (3/3), `BivariateAnalysisParameterRecoveryTests` (7/7), and `MixtureRecoveryTests` (6/6) pass; `NonstationaryValidationTests` passes 1 of 16 cells (most miss the 1% tolerances calibrated for 10,000/5,000 iterations by 1-4%; the linear-trend slope and power-trend cells miss outright) and is left for a decision rather than re-pinned. The four Bayesian "maximum" `CompetingRiskRecoveryTests` cells remain the documented pre-existing failures; the `B17CCovarianceTests` Pearson III cells, the Move3-style uncertain-data bootstrap cell, and the `B17CCensoredCoverageTests` bootstrap cells fail identically at the pre-review commit `7a0a797`. The ARIMA/ARIMAX MAP recovery cells (`Arima111`, `Arimax22`, `Arima22`) failed identically on the pre-review control worktree. An intermittent `UnivariateAnalysisPositivePathReprocessTests.CredibleIntervalWidthChange_EstimatedAnalysis_PreservesResultsReference` race (fire-and-forget versus awaited reprocess) was observed once in the fast suite and is reported, not fixed.
- `docs/release-notes-pending.md` lists the behavior changes for the next BestFit and Numerics release notes; `LegacyDistributionXmlTests` proves the strict distribution parsers against literal RMC.Numerics 2.1.4 payloads; example-project SQLite sidecars are ignored.
- Opened Phase 6 of the verification finalization plan (Batch 6.0, documentation and git only): registered TR-084 through TR-090 for the 21 August results that lacked dispositions (nonstationary default-settings tolerances, Pearson III covariance diagonals, Move3 bootstrap fallback rate, censored-coverage bootstrap cells, re-enabled Bulletin 17C coverage assertions, ARIMA/ARIMAX MAP cells, and the intermittent fast reprocess race); closed TR-078 (reliability grid 14/14) and TR-081 (twelve exact guarded `B17CPenalityTests` reruns, 12/12); restated the TR-052 failure mode (null covariates give an intercept-only prediction); reconciled the mixture (6/6 passed) and competing-risk (six deferred cells named) statuses in the README and chapters; added `time-series.md` to the report book order; rewrote the plan's Phase 6 section as a batch ledger with decision gates and added the Phase 7 closeout section; clarified the technical-reference index wording that collided with the plan's phase numbers.
- Reworded the unpushed checkpoint commit (now `16cc9d9`, the v1.0 report/quick-start PDFs and dependency plots) and untracked `src/RMC.BestFit.Verification/extract_tests.py`; the two modified example projects remain untouched. No production code changed and no unit-test gate was required.
- Phase 6 Batch 6.1 (rating curve), test-first: added `verification/python/rating-curve/generate_rating_curve_example_oracles.py`, which regenerates the three synthetic cases of `examples/6-rating-curve-analysis` from the workbook's stored draws (recipe reproduced to `1e-15`) and writes `rating-curve-example-fixtures.json` (true parameters, replicated default bounds, independent SciPy conditional-MLE optima) and `rating-curve-likelihood-oracle.json` (discharge-space density terms); manifest rows recorded before any C# run. Added `RatingCurveLikelihoodOracleTests`, `RatingCurveContinuityVerificationTests`, `RatingCurveExampleRecoveryTests`, the `RatingCurveExampleFixtures` loader, and the fast TR-045 contract. Guarded confirmation runs: TR-043 cells fail by exactly the change-of-variables sums, TR-044 bound/limit cells fail (lower bound 0), the analytical increment and zero-exponent cells pass, and the TR-045 fast contract fails; the six replication recovery cells are not run pending approval of the acceptance rule. New chapter `docs/verification/rating-curve.md`; no production code changed.
- Phase 6 Batch 6.2 (bivariate evidence consolidation) complete: added `verification/python/bivariate/generate_copula_estimation_oracle.py` and `copula-estimation-oracle.json` (independent closed-form copula densities for AMH, Clayton, Frank, Gumbel, Joe, and Gaussian, each self-checked against the numerical mixed partial of its distribution function; MPL on Weibull complements and IFM on closed-form Normal marginals for the twelve `BivariateDistributionMLETests` fixtures; MPL optima match the historical R targets to about `1e-7`, IFM to within `3.4e-4`), `CopulaEstimationOracleTests` (12/12 through the guarded runner), the bivariate TR-047 routing regression in `AnalysisInformationCriteriaRoutingTests`, and the `docs/verification/bivariate.md` chapter; manifest, book order, README, test inventory, and the technical-reference validation paragraph updated. No production code changed.
- Phase 6 Batch 6.3 (spatial likelihood core), test-first: added `verification/r/spatial-extremes/generate_spatial_copula_oracle.R` and `spatial-copula-likelihood-oracle.json` (R `mvtnorm`; observed-site marginalized Gaussian-copula likelihood with patterned missing sites, complete-row likelihood, and a location-error model with separate observation and Gaussian-process terms; Hosking GEV, exponential correlation on projected coordinates) and `SpatialGEVLikelihoodOracleTests` (8 cells). Guarded confirmation runs: the complete-row, marginal-only, and posterior-kernel cells pass; the missing-site cells reproduce the zero-placeholder value (TR-048 confirmed), the data likelihood carries the process density and the scalar/pointwise identities fail (TR-049 confirmed), and the error-scale score differs between the scalar and pointwise likelihoods (TR-057 confirmed). New chapter `docs/verification/spatial-extremes.md` with the proposed corrections awaiting approval; no production code changed.
- Working tree at the end of this session: the red TR-045 fast contract in `src/RMC.BestFit.Tests/RatingCurve/RatingCurveTests.cs` stays uncommitted until its fix is approved and implemented (the fast core project otherwise passes 3,274/3,275); the two example projects modified by the user and the untracked `src/RMC.BestFit.Verification/extract_tests.py` are untouched. Pending approvals: the Batch 6.1 corrections and replication acceptance rule (`docs/verification/rating-curve.md`) and the Batch 6.3 corrections (`docs/verification/spatial-extremes.md`).
- Phase 6 Batch 6.1 complete (approved 21 August 2026): TR-043 corrected (RatingCurve.cs adds the base-10 change-of-variables term in the scalar, pointwise, and component likelihoods; posterior/MLE unchanged, criteria shift by a data constant), TR-044 corrected (default exponent lower bound and prior minimum 0.1; legacy bounds restored verbatim with a validation warning), TR-045 corrected (aligned-pair positivity error plus a non-blocking unmatched-record warning with counts). Guarded reruns: three TR-043 cells, seven continuity cells, six example-replication cells, and twenty legacy rating-curve recovery cells pass at 1,000 observations; the acceptance rule was amended after the first runs (curve parity with the independent optimum plus a 10% truth band, `1e-4` optimality margin, in-band fraction reported not asserted, sampled MAP from `MCMCResults`) and the recovery sample-size policy (300-1,000 observations, prefer 1,000; no seed search) was adopted and recorded in the plan. Chapter, register, technical reference, appendices, release notes, and manifest updated.
- Phase 6 Batch 6.3 complete (approved 21 August 2026): TR-048 corrected (`GaussianCopula.LogPDF(z, observedSites)` marginalizes unobserved sites through the observed-site correlation submatrix with per-pattern cached factorization; both `SpatialGEV` likelihood paths use it; complete rows bitwise unchanged), TR-049 corrected (`SpatialGEV.PriorLogLikelihood` holds the parameter priors plus the Gaussian-process densities; `DataLogLikelihood` is the observation likelihood; kernel unchanged; criteria of latent-error models exclude the process densities), TR-057 corrected (Godambe sensitivity and variability from the same row/year estimating equations; `null` plus `GodambeCovarianceStatus`/`Diagnostic` on failure, never the variability matrix; parameter validation; reset by `ClearResults`), TR-055 closed (`SpatialGEVAnalysis.ComputeInformationCriteria` with the nonempty row/year unit; WAIC/PSIS-LOO from row/year terms). TR-091 found by the acceptance runs and fixed with approval (`SpatialGEV.Clone()` dropped the copula/error parameter blocks and reset the trend intercepts; the clone now rebuilds its list from the cloned components and copies values, bounds, and priors; three fast contracts). Public API baseline extended additively; the cp1252-corrupted comment characters in `SpatialGEVAnalysis.cs` repaired. Guarded reruns: eight `mvtnorm` oracle cells, the new `SpatialGEVInformationCriteriaTests` cell (MCMC with production defaults), and nine spatial recovery cells pass; fast gates Core 3,299, UI 579, App 438, API 498. Chapter, register, technical reference, appendices, release notes, manifest, and inventory updated.

## 2026-08-20

- Replaced the short-lived full-$K$ mixture sampler and mixture-specific EM-to-MAP/Hessian initializer with the approved identified sampler. `MixtureModel` configuration, all $K$ configured weight priors, public likelihood methods, EM output/covariance, parameter names/counts, and model XML remain full $K$; new mixture `MCMCResults` directly store $(w_1,\ldots,w_{K-1},\boldsymbol\theta)$ and evaluate the full posterior after deriving $w_K=m-\sum_{k<K}w_k$. Numerics source and API were not changed.
- Restored responsibility-based EM weight covariance, including negative off-diagonal covariances, a positive-definite interior $K-1$ block, and the singular full-$K$ public block with zero row sums. EM initialization retains the existing seed, 1.5 covariance multiplier, 20-retry limit, and randomized fallback; there is no mixture MAP refinement, Hessian, chain copy, reflection, intermediate JSON, result conversion, or load-time migration.
- Made diagnostic controls, report sampler summaries, API diagnostic summaries, posterior covariance/correlation, R-hat, and ESS use stored sampled coordinates with component-name index mapping. All $K$ configured prior factors remain in the report and posterior target. `ParameterSetsControl` and scientific post-processing reconstruct physical full-$K$ draws locally, accept legacy full-$K$ results directly, validate retained draws, preserve fitness and stored arrays, and iterate the actual retained output count.
- Deterministic mixture coverage includes prior XML round trip, derived-weight prior enforcement, scalar/pointwise prior identity, K=2/K=3/zero-inflated reconstruction, infeasible rejection, sampler order and storage, exact covariance structure, sampled diagnostic names, physical result display, legacy rows, API summaries, and finite frequency curves. A five-repetition 10,000-draw benchmark measured the identified path at 387.154 ms versus 391.213 ms for the legacy full-$K$ path (0.9896 ratio).
- Strict Debug solution/XML compilation passes with zero warnings and errors when unrelated time-series test compilation is excluded at build evaluation. A subsequent standard build is blocked only by namespace/type and `Transform` ambiguity errors in concurrent `ARIMAXAlignmentTests` work. Mixture-focused Core tests pass 132/132, and the last complete Core binary gate passes 3,195/3,195 including the public-API assertion. UI passes 578/578, App 439/439, and API 498/498. Reflection confirms the committed `MixtureModel`, `MixtureAnalysis`, and `BayesianAnalysis` public API blocks are unchanged. Numerics' focused mixture tests pass 21/21 on .NET 10 and its source tree remains clean. No BestFit Verification method was run.

## 2026-08-04

- Completed the method-level ownership audit of all 84 C# files in `RMC.BestFit.Verification`. Verification now contains 435 recovery, coverage, analytical, independent, external-package, published, or real-source methods instead of 1,196 mixed-scope methods; 761 duplicate unit, estimator-smoke, and ineffective assertions were removed. Recovery/oracle-specific class names replace the former mixed time-series, rating-curve, spatial, and model-estimation files.
- Added 35 missing deterministic contracts to `RMC.BestFit.Tests` with inline fixtures and no Verification data dependency. Coverage includes Bivariate and SpatialGEV cancellation/validation, RatingCurve calculations and XML round-trip, PSIS caching, GMM state/guards, and MAP reset/argument guards. No production source, algorithm, seed, tolerance, likelihood, or public API changed.
- The post-cleanup fast gates pass Core 3,175/3,175, UI 571/571, App 428/428, and API 496/496. Strict Debug solution compilation, including Verification compilation with enforced XML documentation, passes with zero warnings/errors. Instrumented Core coverage excluding the live USGS download and stopwatch-only large-frame test passes 3,173/3,173 and improves line/branch/method coverage from 64.73/59.46/86.94% to 66.03/60.43/88.25%. No Verification method or full suite was executed.
- Historical note: this checkpoint temporarily added a mixture-specific bounded MAP/Hessian refinement after EM. That initializer was superseded and removed by the 20 August identified-sampler correction above.
- The temporary initializer's dedicated Verification method was removed with it. The three Bayesian mixture recovery methods were not rerun at this checkpoint and remain pending separately authorized focused reruns after the 20 August parameterization change.
- Strict Debug solution compilation passes with zero warnings/errors. The changed test groups pass `MixtureAnalysisTests` 36/36 and `MaximumAPosterioriTests` 11/11. The independent Composite finite oracle now uses the approved user-visible contract, `XTransform.None`, matching `CompositeAnalysis` without changing its `1E-10` tolerance. The final fast gates pass Core 3,140/3,140, UI 571/571, and App 428/428.

## 2026-08-03

- Implemented the Phase 4 competing-risk/composite recovery supplement without adding public APIs. Added 20 exact competing-risk methods covering eight pinned Numerics min/max fixtures and two latent-correlation-0.6 extensions through MLE and Default DEMCzs, plus ten composite methods covering R `mistr` Table 45, analytical mixture/min/max identities, bracketing, bivariate-Normal orthants, and three complete 20-by-20-by-20 Cartesian posterior oracles.
- Added the authorized `CompetingRiskAnalysis` MAP-centered MCMC initializer while preserving every Default DEMCzs sampling, proposal, output, interval, point-estimator, and seed setting. Default Differential Evolution supplies the mode; the bounded posterior Hessian supplies covariance; singular information uses an initialization-only regularized Moore-Penrose covariance; the full population uses the fixed 1.5 inflation and sampler seed; failed initialization resets cleanly to randomized starts. Fast tests cover deterministic finite population construction, best-state chain seeding, dimension rejection, and the singular public-covariance versus initialization-covariance distinction.
- Pinned Numerics `c361f2864428a98a33d6072ffa9bc11ac360839d` and RMC-TotalRisk `d4d43e6407ddb4219e5cd7f613e80f749a3a0ab7`. All 30 new methods were rerun individually through the guarded runner after MAP initialization: all ten competing-risk MLE methods passed, four of ten Default-DEMCzs methods passed, and nine of ten Composite methods passed. MAP initialization resolved the former separated three-Weibull R-hat failure, while the correlated two-Weibull cell now fails ESS 77.5203; the total remains 23 passes and seven unresolved findings, so Phase 5 remains gated. The two existing TR-014 methods also passed their prior focused runs.
- The mixed-family competing-risk fixture exposed a Numerics `LogNormal.Clone()` defect that dropped the configured logarithm base. Numerics commit `e57af20` preserves `Base`; clone-contract tests cover `LogNormal` and the already-correct `LogPearsonTypeIII`. Numerics Release built with zero warnings/errors and all 2,072 tests passed on each of net481, net8.0, net9.0, and net10.0. The BestFit mixed-family MLE then passed without a local workaround.
- Confirmed that Competing Risks already enforces the same one-to-three component identifiability guard as Mixture at constructor and model validation. Added a fast analysis-level validation test proving a fourth component is rejected through `CompetingRiskAnalysis`.
- Re-ran the mandatory fast regression gates after the final MAP-initialization source changes: Core 3,134/3,134, UI 571/571, and App 428/428 pass with zero build warnings. The complete Verification project was never run.
- Reconciled all Phase 0-4 verification and technical-reference documentation against BestFit `9d252f6`, the pinned Numerics and TotalRisk sources, the 16 manifest artifacts, and the current evidence ledger. Phase 0 remains operational with test-ownership work active; Phases 1-3 and the original Phase 4 findings are closed for their approved scopes, while the recovery supplement remains gated by seven unresolved findings.
- Corrected stale reviewer appendices, API traceability, handoff text, PSIS tolerances, dependency hashes, and repository-wide gate counts. Bulletin 17C remains eligible for Equal/AIC/BIC/RMSE composite weighting and is zero-weighted rather than type-rejected when posterior-only criteria are unavailable.
- Validated the reconciliation with all 16 manifest hashes, Numerics .NET 10 Release 2,024/2,024, strict XML and Release builds, Core 3,116/3,116, UI 568/568, App 428/428, the five documentation contracts, the explicit public API baseline, and Verification compilation. No Verification method or PDF build was run.
- Closed TR-012 with a focused Numerics correction (`cafe6cf3837988341912a5aa8bfda444ea55ff77`) that preserves the independent golden seed sequence and routes the existing `CompetingRisks.GenerateRandomValues` signature through dependency-aware simulation. Numerics Release build and all 2,024 tests passed on the validated .NET 10 target.
- Added four narrowly scoped competing-risk verification methods. Independent, perfectly positive, perfectly negative, and correlation-matrix modes each passed separately through the exact guarded runner against Gaussian-copula Spearman-rank and analytical maximum-CDF targets; the full Verification project was not run.
- Closed TR-013 with explicit invalid/unavailable criterion classification, exact-zero weights for excluded children, named diagnostics, an all-invalid failure, and a division-free exact-zero RMSE branch. Bulletin 17C is not type-rejected: it participates in Equal/AIC/BIC/RMSE and receives zero weight with a warning only when a selected posterior criterion is unavailable and another child is usable.
- Closed TR-015 by adding the authorized defensively owned `CorrelationMatrix` property to core and UI composite analyses, validating structure/dimension/positive definiteness, preserving optional legacy loading, serializing invariant XML and SQLite data, and propagating the matrix into point estimates and frequency results without changing existing public method signatures.
- Closed TR-014 with independent, full-range, without-replacement posterior index rows over the shortest actual retained count in Composite and CFA. CFA caches its semantic copula/X/Y mapping for indexed access; UI save/open/copy/undo preserves the existing nonnegative seed. `BivariateAnalysis` remains unchanged and conditional on fixed marginals. Legacy saved Composite/CFA summaries require reprocessing.
- Passed both exact guarded TR-014 methods: the Composite empirical-Cartesian oracle in 2.677 s and the CFA closed-form Normal-sum oracle in 2.505 s. Mean tolerance is `0.02`, credible-limit tolerance is `0.05`, and both deliberately raw-paired controls miss by at least `0.10`. The full Verification project was not run.
- Passed the strict Debug XML-documentation solution build with zero warnings/errors, the exact public API baseline, Verification compilation, and all mandatory fast suites before the MAP-initialization follow-up. Final follow-up validation is recorded above.

## 2026-07-31

- Implemented the TR-006/TR-007/TR-008 mixture correction. BestFit keeps the full-$K$ model, prior, public likelihood, and project-model contract while the sampler stores the identified $K-1$ coordinates; likelihood evaluation copies and normalizes all $K$ weights without mutating caller arrays.
- Corrected the exact-zero positive-hurdle law across density, CDF, quantiles, simulation, mixed-observation likelihoods, and EM; exact atom estimation uses exact annual records only, and impossible EM rows now fail with row context.
- The focused Numerics mixture batch is reachable as `3e69a93`; the current normalized .NET 10 Release gate passes 2,024/2,024 with zero warnings, errors, or failures. The correction was also validated across all configured target frameworks before subsequent tests were added.
- Added BestFit fast coverage and three compiled recovery-parity methods. The strict XML Debug build, Release solution build, public API baseline, and Verification compilation pass with zero warnings/errors; normalized fast suites pass Core 3,116/3,116, UI 568/568, and App 428/428.
- Closed the Phase 4 mixture subset after six exact guarded runs: three BestFit-generated Numerics/BestFit recovery-parity methods and three BestFit-generated Bayesian `MixtureAnalysis` recovery methods all passed with one TRX each. Bayesian closeout includes posterior recovery, label ordering, finite split R-hat below 1.1, conservative ESS above 100, and the positive-hurdle atom check.
- Full-$K$ legacy mixture posterior results remain directly readable. The short-lived $K-1$ persisted form is not supported. At this 31 July checkpoint, Phase 4 remained open for TR-014; the 3 August entry above records its subsequent closure.
- Closed the Phase 4 point-process subset. TR-004 is complete after both independent mixed-likelihood cells passed; the seasonal fixture was corrected from an all-above threshold to one below/two above without changing production code or the `2E-7` tolerance.
- Re-ran all Bayesian recovery fixtures with the untouched `BayesianAnalysis` defaults and 1,000 observations. Calendar-year uniform recovery, nonseasonal production recovery, and seasonal production recovery passed; the former seasonal second-Kappa miss disappeared. The water-year recovery failure was a verification-coordinate error: it changed `K1/K2` from `170/350` to `80/260` instead of changing only the block origin. Holding the block-day parameters fixed and shifting only the dates to an October water year passed the exact method. All ten point-process cells now pass without a production formula, sampler default, prior, or tolerance change.

## 2026-07-30

- Implemented the TR-004/TR-005 point-process batch: preserved source exposure, retained manual year/index-span fallback and warnings, separated empirical count/rate from fitted intensity, and corrected fixed-size and duration simulation to the approved empirical-Lambda Poisson/Hosking-GPA process.
- Added the fast monthly-histogram changepoint-prior heuristic with calendar/water-year rotation, month-length flatness guard, circular smoothing, deterministic valleys, five-month windows, broad fallbacks `[1,251)` and `[200,367)`, custom-prior preservation, and ordinary parameter serialization.
- Kept all six seasonal GEV defaults, likelihood equations, DEMCzs settings, tolerances, and seed contracts unchanged.
- Consolidated independent exponential-clock Poisson, analytical Hosking-GPA, PERT placement, and uniform recovery fixtures. Removed the intentionally failing PERT recovery experiments after they demonstrated the expected interior-timing mismatch.
- Corrected the water-year automatic-prior recovery fixture to use shifted block-day changepoints 80 and 260 rather than calendar coordinates 170 and 350.
- Audited `PointProcessModel`, `PointProcessAnalysis`, fast tests, and focused verification for state bugs, stale XML, encoding damage, low-value narration, and obsolete fixture references. Strict builds, all fast suites, and Verification compilation pass with zero warnings.
- Passed the strict Debug solution build with `EnforceXmlDocumentation=true`, the Release solution build, and all mandated fast suites: Core 3,116/3,116, UI 568/568, and App 428/428. The `validate-code-xml-docs.ps1` wrapper named in `AGENTS.md` is not present in the tracked scripts inventory, so its private-method scan could not be invoked; the touched point-process files were audited directly.
- Resolved the seasonal point-process input and annualization decisions: every exact seasonal POT observation now requires a valid date, while annual/block-indexed uncertain, interval, and threshold records use the maximum of the two independent exposure-adjusted seasonal processes. Exposure fractions weight process intensities, not annual mixture probabilities. All ten exact current-source point-process verification methods subsequently passed under repository policy.

## 2026-07-28

- Closed TR-003 as a documentation-only decision: grouped perception thresholds use the existing deterministic earlier-below/terminal-above disaggregation after preserving explicit indexes, and distribution-dependent priors are evaluated at the last, most-recent observed time step consistent with the published quantile-prior workflow. No code or permutation-invariance change was required.
- Closed TR-016 as a documented architecture decision: Bulletin 17C reuses the Bayesian analysis result-storage shape for stable persistence and reprocessing, with GMM/frequentist meanings for legacy member names and no code/API redesign.
- Closed TR-020 in its approved unit scope by guarding Cohn intervals and asymptotic variance to exact-data LP3; all unsupported parent/data cases now fail explicitly, while numerical Cohn verification remains deferred.
- Closed TR-021 with seven exact formal Bulletin 17C worked-example runs: `Test_Example1` through `Test_Example7` all passed published LP3 mean, standard deviation, and skewness comparisons at absolute tolerance `1E-3`.
- Reviewed and repaired XML documentation/comments throughout `Bulletin17CDistribution` and `Bulletin17CAnalysis`, including damaged mathematical notation, stale Bayesian wording, malformed XML, and low-value comments; no AI/tool breadcrumbs remain.
- Closed TR-017 without renaming `BiasCorrectedBootstrap`; technical documentation identifies it as the second-order bias-corrected pivotal bootstrap and distinguishes it from scalar BC/BCa intervals.
- Reworked Bulletin 17C bootstrap initialization with bounded midpoint, ROS, distribution-default, and parent starts ranked against the same penalized GMM target; confirmed-converged iterative GMM results are accepted even when the final inner pass reaches its evaluation cap.
- Kept Yeo-Johnson pivotal links and added post-inverse parameter repair against existing model bounds; Example 5 replicate 695 exercises the skew-bound repair without throwing.
- Removed the obsolete Mahalanobis refit rejection after the guarded sweep showed it caused every remaining outer retry. Fourteen exact Examples 1-7 ordinary/pivotal cells then produced 13,000 unguarded finite outputs from 13,000 realizations with zero retries, substitutions, failed candidates, optimizer fallbacks, and BestFit/Numerics first-chance exceptions.
- Added and pinned the Examples 5-7 reliability cells (500 outputs per method for highly censored Example 7) and retained the parent-fit fallback solely to guarantee configured downstream output length.

## 2026-07-17

- Prepared v2.0.0 release metadata: switched central dependencies to `RMC.Numerics` 2.1.4 and `RMC.Wpf.Framework.*` 1.0.4, removed the local project-reference override file, and synchronized final UI/App/project metadata to `2.0.0`.
- Added official release messaging for the PR body, GitHub Release body, LinkedIn post, and screenshot gallery suggestions in `docs/release-messaging-v2.0.0.md`.
- Updated release-facing README, documentation index, and example index wording from beta/pre-release language to official v2.0.0 release language.
- Added a BestFit-specific release workflow to local `AGENTS.md` and `CLAUDE.md`; both files are ignored by the repository unless force-added intentionally.
- Validated release readiness with `dotnet restore`, Debug and Release builds, all four fast test projects, and `dotnet pack src/RMC.BestFit/RMC.BestFit.csproj -c Release -o packages /p:Version=2.0.0`.
- Inspected `packages/RMC.BestFit.2.0.0.nupkg`: package id/version are `RMC.BestFit`/`2.0.0`, release notes are present, README and LICENSE are included, and the package dependency metadata points to `RMC.Numerics` 2.1.4.
## 2026-07-16

- Diagnosed the B17C bootstrap regression: assigning a resampled frame through the public `Bulletin17CDistribution.DataFrame` setter ran `SetDefaultParameters`, which wiped the cloned parent initials, disabled every parameter penalty (silently dropping regional-skew prior propagation), and — when default-parameter derivation threw for threshold-heavy boot frames — emptied the parameter list so every replicate failed and fell back to the parent (uncertainty collapsed to a point mass; the progress bar sat at 1% while retries crawled).
- Fixed GMM `Status` coherence: multi-pass strategies restore `Status = Success` when abandoning a failed refinement pass and returning an earlier valid solution; `Estimate()` resets stale outputs up front.
- Added `Bulletin17CDistribution.CloneWithDataFrame` (XElement round-trip preserves parameters, penalties, and links) and warm-started every bootstrap replicate at the parent fit; aligned the replicate acceptance gate with the parent fit (reject only hard failures and non-finite estimates).
- Redesigned failure handling across the uncertainty samplers: failed or rejected realizations are discarded after retries — never substituted with the parent vector — with abort guards (fewer than two survivors or >50% discards) surfaced through the new `UncertaintyDiagnosticMessage` and the UI warning message.
- Extended `BootstrapDiagnostics` (retained count, transform failures, per-attempt GMM status distribution; backward-compatible XML), made the plain MVN sampler report the same diagnostics, rewrote the report section for discard semantics with retention warnings, and persisted diagnostics with the analysis.
- Fixed sampling-loop progress: first tick after the first completed replicate (`AnalysisProgress.ShouldReportLoopProgress`) and monotone pivot-phase mapping (55/56/44) replacing the backwards jump.
- Hardened WPF dispatch: `B17CAnalysisPropertiesControl.Element_PropertyChanged` now marshals to the dispatcher; the UI wrapper suppresses its own run's mid-flight `ThresholdSeries` recompute notification (narrow guard) so `ClearResults` cannot fire on a worker thread.
- Verified end-to-end with a headless harness on B17C Examples 1 and 4: penalties now survive on all boot clones and previously all-failing threshold-heavy replicates fit successfully; all four unit suites pass with zero warnings under XML-docs-as-errors builds.

## 2026-07-14

- Replaced the `MainProjectNode` local Quick Start Guide PDF action with the online RMC-BestFit User Guide.
- Added a testable connectivity and default-browser launcher that reuses `TimeSeriesDownload.IsConnectedToInternet()`.
- Added deterministic App tests for online, offline, validation, failure propagation, and Help-menu wiring behavior.
- Added main-branch Technical Reference and Example Projects links through the generalized online Help launcher.
- Added active-development note boxes to the technical-reference and example-project indexes.
- Reserved the type-compatible `HelpImage` resource for the User Guide and separated the text-only online resources from application items.
- Replaced the framework's default Terms and Conditions document with the verbatim 0BSD license and optional Zenodo citation guidance using a copyable, clickable concept-DOI URL.
- Corrected the `YeoJohnsonLink.FitLambda` XML reference to use the Numerics method's `IList<double>` signature.
- Added a README Documentation callout identifying the RMC-BestFit 2.0 documentation and verification materials as under active development.
- Validated the affected App projects with zero warnings and all fast Core, UI, and App tests passing.
- The required repository XML validation script is absent; equivalent scoped XML-as-errors builds passed for all affected projects.
